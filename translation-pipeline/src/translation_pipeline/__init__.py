"""CSTC-4 deterministic Arabic-translation data-entry pipeline.

Task 1 delivers the project scaffold, the standalone data-store schema and
interrupt-safe store I/O. Scrape (step 1), the no-code translation step (step 2),
the validator and write-back (step 3) are added in later tasks.
"""

__version__ = "0.1.0"
